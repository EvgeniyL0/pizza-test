{
  "name": "pizza-test",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy-github": "push-dir --dir=dist --branch=gh-pages --cleanup",
    "heroku-postbuild": "npm run build"
  },
  "dependencies": {
    "core-js": "^3.6.5",
    "nuxt": "^2.14.6",
    "push-dir": "^0.4.1"
  },
  "devDependencies": {
    "sass": "^1.32.7",
    "sass-loader": "10.1.1"
  }
}
